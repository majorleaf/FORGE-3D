# Basic Redis config for FORGE3D

# Persistence
save 900 1
save 300 10
save 60 10000

# Memory limit
maxmemory 256mb
maxmemory-policy allkeys-lru

# Logging
loglevel notice

# Bind to all interfaces inside Docker
bind 0.0.0.0
protected-mode no